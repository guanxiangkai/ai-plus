pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories { mavenCentral() }
}

rootProject.name = "artifact-plus"
include("artifact-plus-core", "artifact-plus-gradle-plugin", "artifact-plus-maven-plugin")
